using HelloApp;

/*IpadFactory ipad = new();

IphoneFactory iphone = new();

Client.doSomething(ipad);

Client.doSomething(iphone);*/



/*Func<stylesOfFurnuture, (IChair chair, ISofa sofa)?> func = (style) =>
{
    if (style == stylesOfFurnuture.Modern)
    {
        var factory = new ModernFactory();

        return (factory.createChair("CHAIR"), factory.createSofa("SOFA"));
    }

    if (style == stylesOfFurnuture.Victorian)
    {
        var factory = new VictorianFactory();

        return (factory.createChair("CHAIR"), factory.createSofa("SOFA"));
    }

    return null;
};

var furnuture = func(stylesOfFurnuture.Victorian);

var chair = furnuture != null ? furnuture.Value.chair : null;*/

